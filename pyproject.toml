[project]
name = "aris-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.3"
dependencies = [
    "pillow>=12.0.0",
    "pyyaml>=6.0.3",
    "ruamel-yaml>=0.19.0",
    "numpy<2.0.0",
    "inquirer>=3.0.0",
    "google-cloud-firestore>=2.23.0",
]

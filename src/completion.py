# ---------------------------------------------------------------------------
# File: src/completion.py  (DEPRECATED — replaced by Click-native completion)
# ---------------------------------------------------------------------------
"""This module is no longer used.

Shell completion is now handled by Click in src/cli.py.

Setup instructions for your shell:

  bash:  eval "$(_ARIS_COMPLETE=bash_source aris)"   (add to ~/.bashrc)
  zsh:   eval "$(_ARIS_COMPLETE=zsh_source aris)"    (add to ~/.zshrc)
  fish:  _ARIS_COMPLETE=fish_source aris | source    (add to ~/.config/fish/completions/aris.fish)
"""